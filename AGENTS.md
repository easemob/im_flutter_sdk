# AGENTS.md

## 核心原则

1. **满足当前需求** → 设计以已确认需求为边界，不为“未来可能需要”提前加字段、参数、流程或抽象。
2. **短小精悍** → 模块、函数、文档都应尽量小而清晰；能用简单结构解决的问题，不引入重框架。
3. **低耦合可拆分** → 模块只暴露必要接口，可以独立理解、测试、替换和组合。
4. **少即是稳** → 删除未使用的代码、配置和概念；保留的内容必须有当前用途或明确决策依据。
5. **避免预测式扩展，保留结构性灵活** → 不为未确认的未来需求提前实现字段、参数、流程或抽象；但当前实现应保持边界清晰、职责单一、易测试、易替换，确保未来变化发生时可以低成本调整。
6. **独立判断** → 不盲从任何讨论意见。遇到架构、范围、复杂度、长期维护成本相关问题时，必须基于需求和工程事实提出判断；如果发现方案有风险，应直接说明风险和替代方案。
7. **决策需确认** → 对会影响架构、数据结构、接口、目录、技术路线或长期维护成本的选择，不应直接替用户决定；应给出推荐方案、理由和取舍，等待确认后再落地。

## 开发习惯

1. **单次提交** → 如果使用 superpowers:writing-plans 创建实现计划时，所有任务完成后统一提交，不进行中间提交
2. **子代理模型选择** → 如果使用 superpowers:subagent-driven-development，推荐使用 sonnet 4.6 或 gpt-5.4（如果使用 Anthropic 或 OpenAI 系列模型）
3. **计划文档状态更新** → 如果使用 superpowers:subagent-driven-development，在所有任务完成、所有检查完成、提交代码之前，更新实现计划文档的任务状态（复选框），然后与代码一起提交
4. **任务依赖关系** → 如果使用 superpowers:writing-plans，在计划文档末尾添加任务依赖关系列表；如果是子代理执行，可以根据依赖关系考虑并发执行
5. **有草稿设计的情况下写spec** → 如果使用 superpowers 对于已有设计草稿的情况，先批量确认简单明确的部分（快速推进），然后集中讨论需要决策的复杂问题，避免碎片化的单问题确认
6. **文档语言** → 如果使用 superpowers 编写 spec 或实现计划时，请使用中文
7. **git worktree 分支命名** → 如果使用 superpowers:using-git-worktrees，分支名字使用实现计划文档的名字
8. **项目文档入口** → 继续项目工作时，阅读 `AGENTS.md` 后，如任务涉及需求、设计、计划、实现依据或文档沉淀，必须继续阅读 `docs/README.md`
9. **中文编写** 如果使用 superpower 请使用中文编写 spec  和 plan 等文档。
