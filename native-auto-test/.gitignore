# Editor and OS
.DS_Store
.cursor/
.idea/

# Reports / outputs
allure-results/
report.html

# Python
__pycache__/
*.pyc
.venv/

out/

# Local config with secrets (legacy root file, no longer read by code)
config.yaml

# Local environment configs. Templates and the non-sensitive bridge file stay in Git.
config/*.yaml
!config/bridge.yaml

# Local WebSocket relay runtime state
.local/
